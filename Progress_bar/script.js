(function () {
    const get = (target) => {
        return document.querySelector(target);
    };

    const onscroll = () => {
        
    }

    window.addEventListener("scroll", () => onscroll())

})();
